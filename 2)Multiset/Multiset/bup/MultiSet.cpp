#include "MultiSet.h"





void execute() {




}














/*
template <typename T>
void StatMultiSet::addNum(T value) {
	if (inSet.empty()) {
		min = value;
		max = value;
	}
	else {
		if (value>max) max = value;
		if (value<min) min = value;
	}
	inSet.insert(value);
};
template <typename T>
void StatMultiSet::addNum(const std::vector<T> &vec) {
	for (auto it : vec) {
		addNum(it);
	}
};
template <typename T>
void StatMultiSet::addNum(const std::list<T> &lis) {
	for (auto it : lis) {
		addNum(it);
	}
};
template <typename T>
void StatMultiSet::addNum(const std::multiset<T> &mSet) {
	for (auto it : mSet) {
		addNum(it);
	}
}
*/